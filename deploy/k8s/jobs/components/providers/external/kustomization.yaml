apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# External clusters inject pod labels, node selectors, tolerations, CSI drivers,
# and volume attributes at deploy time from scripts/deploy.ps1.
patches: []
